import { CourseFormPage } from "@/components/dashboard/editorial-pages";

export const dynamic = "force-dynamic";

export default function Page() {
  return <CourseFormPage formItem={{}} isEdit={false} />;
}
